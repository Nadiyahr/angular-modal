/// <reference types="react-scripts" />

type OptionType = {
  value: string;
  label: string;
};

type Obj = {
  name: string,
  email: string,
  date: string,
  time: string,
  selectedOption: string,
};
